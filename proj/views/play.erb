<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Play</title>
</head>
<body>
  <h1>Questions for <%= @system %> System</h1>

  <% if locals[:message] %>
    <p><%= locals[:message] %></p>
  <% end %>

  <form action="/play/question" method="post">
    <p><%= @current_question.text %></p>
    <% @current_question.options.each do |option| %>
      <input type="radio" name="option_id" value="<%= option.id %>"> <%= option.text %><br>
    <% end %>
    <input type="submit" value="Submit">
    
  </form>
</body>
</html>