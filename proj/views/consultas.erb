<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Consultas - AnatomyEasy</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f0f8ff;
      color: #333;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .container {
      text-align: center;
      background-color: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
      max-width: 450px;
      width: 100%;
    }
    h1 {
      color: #2c3e50;
      margin-bottom: 20px;
    }
    label {
      display: block;
      font-size: 1em;
      color: #333;
      margin-top: 15px;
    }
    select, button {
      width: 100%;
      max-width: 250px;
      padding: 8px;
      margin-top: 10px;
      border-radius: 5px;
      border: 1px solid #ccc;
      font-size: 0.9em;
    }
    button {
      background-color: #3498db;
      color: white;
      cursor: pointer;
      margin-top: 20px;
    }
    button:hover {
      background-color: #2980b9;
    }
    form {
      display: flex;
      flex-direction: column;
      align-items: center;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Consultas de Preguntas</h1>
    <form action="/admin/consultas/resultado" method="post">
      <label for="cantidad">Listar la/s</label>
      <select id="cantidad" name="cantidad" required>
        <% (1..60).step(1).each do |i| %>
          <option value="<%= i %>"><%= i %> preguntas</option>
        <% end %>
      </select>
      <label for="tipo">que han sido contestadas la mayor cantidad de veces de manera:</label>
      <select id="tipo" name="tipo" required>
        <option value="incorrectas">incorrecta</option>
        <option value="correctas">correcta</option>
      </select>

      <button type="submit">Consultar</button>
      <button type="button" class="back-button" onclick="window.history.back()">Volver</button>
    </form>
  </div>
</body>
</html>
